<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dimine.taglib.dao.CheckboxDao">
	<select id="selectcheckboxlist" resultType="java.util.HashMap" parameterType="com.dimine.taglib.entity.CheckboxEntity">
		select ${checkboxid},${checkboxtext} from ${tablename} where ${parameter}
	</select>
</mapper>